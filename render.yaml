services:
  - type: web
    name: pasien-app
    env: docker
    plan: free
    branch: main
    dockerfilePath: Dockerfile
    buildCommand: ""
    startCommand: "apache2-foreground"
    autoDeploy: true
